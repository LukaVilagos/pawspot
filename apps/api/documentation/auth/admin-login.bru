meta {
  name: admin/login
  type: http
  seq: 3
}

post {
  url: {{apiUrl}}{{routeRoot}}/admin/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe+22@example.com",
    "password": "123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
