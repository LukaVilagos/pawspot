meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/{{root}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Max",
    "species": "Dog",
    "age": 5,
    "sanctuaryId": "{{sanctuaryId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
