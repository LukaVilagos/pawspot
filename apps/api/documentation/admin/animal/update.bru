meta {
  name: update
  type: http
  seq: 2
}

put {
  url: {{apiUrl}}/{{root}}/{{animalId}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Max Updated",
    "species": "Golden Retriever",
    "age": 6
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
