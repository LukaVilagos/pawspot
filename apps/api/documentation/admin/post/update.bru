meta {
  name: update
  type: http
  seq: 2
}

put {
  url: {{apiUrl}}/{{root}}/{{postId}}
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Updated Post Title",
    "content": "This is the updated content."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
