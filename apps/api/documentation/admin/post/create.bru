meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/{{root}}
  body: json
  auth: inherit
}

body:json {
  {
    "title": "My First Post",
    "content": "This is the content of my first post.",
    "userId": "{{userId}}",
    "sanctuaryId": "{{sanctuaryId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
