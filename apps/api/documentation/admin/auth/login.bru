meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/{{root}}/admin/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe+22@example.com",
    "password": "123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
