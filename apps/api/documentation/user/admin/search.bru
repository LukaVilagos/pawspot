meta {
  name: search
  type: http
  seq: 5
}

post {
  url: {{apiUrlAdmin}}{{routeRoot}}/search
  body: json
  auth: inherit
}

body:json {
  {
    "limit": 2,
    "page": 3
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
