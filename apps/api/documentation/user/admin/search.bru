meta {
  name: search
  type: http
  seq: 5
}

post {
  url: {{apiUrlAdmin}}{{routeRoot}}/search
  body: json
  auth: inherit
}

body:json {
  {
    "filter": [
      ["type", "ADMIN"]
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
