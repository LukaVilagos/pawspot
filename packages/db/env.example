```text
# Database configuration example (DO NOT commit secrets)
# Copy this file to `packages/db/.env` or to the repo root `.env` for local development
# Fill in real values only on your machine (the `.env` file is ignored by git)

# Example DATABASE_URL format:
# postgres://<DB_USER>:<DB_PASSWORD>@<HOST>:<PORT>/<DB_NAME>?schema=public
DATABASE_URL="postgresql://<DB_USER>:<DB_PASSWORD>@localhost:5433/<DB_NAME>?schema=public"

# Optionally you can set Postgres individual vars used by docker-compose:
# POSTGRES_USER=<DB_USER>
# POSTGRES_PASSWORD=<DB_PASSWORD>
# POSTGRES_DB=<DB_NAME>
```